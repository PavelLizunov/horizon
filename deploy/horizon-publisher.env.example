# Install as /etc/horizon-publisher.env on the ingress host.
# This file contains paths and limits only; no credentials.
HORIZON_SITE_ROOT=/srv/digest.example.com
HORIZON_AUDIO_ROOT=/srv/audio.example.com
HORIZON_AUDIO_MAX_BYTES=2147483648
HORIZON_MAX_OPUS_BYTES=52428800
